sudo: required
language: generic
serivces:
  -docker

before_install:
  - docker build -t  Mehrnaz-a/docker-react  -f Dockerfile.dev .

script:
  - docker run -e CI=true Mehrnaz-a/docker-react npm run test